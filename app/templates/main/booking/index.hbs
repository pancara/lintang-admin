<div class="container-fluid">
    <div class="row">
        <div class="col-lg-12">
            <div class="pw-panel pw-panel-align-right">
                <button class="btn btn-xs" {{action "toggleFilter"}}>
                  {{#if shownFilter }}
                      Hide Filter &nbsp;
                    {{fa-icon "caret-up"}}
                  {{else}}
                      Show Filter &nbsp;
                    {{fa-icon "caret-down"}}
                  {{/if}}
                </button>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-lg-12">
          {{pw-booking-filter
          register-as=filterBooking
          shown=shownFilter
          isZebra=true
          bookingStatuses=bookingStatuses
          orderTypes=orderTypes
          applyFilter=(action "applyFilter")
          }}
        </div>
    </div>

    <div class="row">
        <div class="col-lg-12">
          {{pw-table data=data
          headerTemplate="main/booking/header"
          footerTemplate="main/booking/footer"
          masterTemplate="main/booking/row"
          detailTemplate="main/booking/row-detail"
          isZebra=false
          isBordered=true
          cancelBooking=(action "cancelBooking")
          confirmPayment=(action "confirmPayment")
          }}

          {{pw-paging register-as=paging refresh=(action "refresh")}}
        </div>
    </div>
</div>

